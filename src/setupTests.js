// This is a global setup that will run before executing tests.
// We may decide to remove it later if we don't need a global setup for tests
// @Nick

import '@testing-library/jest-dom/extend-expect';